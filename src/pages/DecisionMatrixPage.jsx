import React from 'react';
/*import { useParams } from 'react-router-dom';*/
import Header from "../components/Header";

function DecisionMatrixPage() {
    /*
    const {walkscore} = useParams();
    const {price} = useParams();

    var score;

    for (let i = 0; i < favHouses.size(); i++) {

        score = walkscore * favHouses.get(i).getWalkscoreScore() + price * favHouses.get(i).getPriceScore();
        favHouses.get(i).setScore(score);

    }

    favHouses.sort(function(a, b) {

        return a.score - b.score;

    })
    */

    return(

        <div>
        <Header/>
        
        </div>

    ) 

        

    

}


export default DecisionMatrixPage